# ============================================================
# Finance Tracker — Backend Environment Variables
# ============================================================
# The canonical reference for all environment variables is the
# .env.example file at the repository root (../../.env.example).
#
# Copy ../../.env.example to packages/backend/.env and
# populate the values for local backend-only development.
# Never commit .env to source control.
